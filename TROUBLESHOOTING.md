# 🔧 Решение проблем

## Проблема: "Не работает" или "Ничего нельзя нажать"

### Решение 1: Проверьте консоль браузера

Откройте DevTools (F12) и проверьте вкладку Console на наличие ошибок.

### Решение 2: Режим разработки

Приложение теперь поддерживает режим разработки:
- Если нет Telegram user данных, создается моковый пользователь
- Backend автоматически создает пользователя при первом запросе (в режиме разработки)

### Решение 3: Проверьте серверы

```bash
# Проверка backend
curl http://localhost:3000/health

# Проверка frontend
curl -k https://localhost:5173
```

### Решение 4: Перезапуск

```bash
# Остановить все
pkill -f "tsx watch"
pkill -f "vite"

# Запустить заново
cd backend && npm run dev &
cd ../frontend && npm run dev &
```

## Проблема: Ошибки аутентификации

### В режиме разработки (вне Telegram):

1. Откройте приложение в браузере: `https://localhost:5173`
2. При первом запросе backend автоматически создаст тестового пользователя
3. Приложение должно работать с моковыми данными

### В Telegram:

1. Убедитесь, что используете публичный HTTPS URL (не localhost)
2. Проверьте, что туннель (cloudflared) запущен
3. Проверьте логи backend на наличие ошибок

## Проблема: Элементы не кликабельны

### Проверьте:

1. Откройте DevTools (F12)
2. Проверьте вкладку Console на ошибки JavaScript
3. Проверьте вкладку Network на ошибки запросов

### Возможные причины:

- Ошибки JavaScript блокируют выполнение
- Проблемы с CORS
- Ошибки API запросов

## Проблема: Ngrok/Cloudflare не работает

### Cloudflare Tunnel:

```bash
# Запустить вручную
cloudflared tunnel --url https://localhost:5173

# Проверить логи
tail -f /tmp/cloudflared.log
```

### Альтернатива - локальная разработка:

Для тестирования без Telegram можно использовать:
- `https://localhost:5173` в браузере
- Приложение будет работать с моковыми данными

## Логи для отладки

```bash
# Backend логи
tail -f /tmp/backend.log

# Frontend логи  
tail -f /tmp/frontend.log

# Cloudflare логи
tail -f /tmp/cloudflared.log
```

## Частые ошибки

### "User not found"
- В режиме разработки пользователь создается автоматически
- Проверьте, что backend запущен

### "Failed to authenticate"
- Проверьте заголовки запросов в Network tab
- Убедитесь, что `X-Telegram-Id` отправляется

### CORS ошибки
- Проверьте `FRONTEND_URL` в `backend/.env`
- Должен совпадать с URL frontend (включая https://)

## Полная переустановка

```bash
# Остановить все
pkill -f "tsx watch"
pkill -f "vite"
pkill cloudflared

# Очистить и переустановить
cd backend && rm -rf node_modules && npm install
cd ../frontend && rm -rf node_modules && npm install

# Перезапустить
cd ../backend && npm run dev &
cd ../frontend && npm run dev &
```

