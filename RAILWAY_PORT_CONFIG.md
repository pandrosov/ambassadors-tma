# 🔧 Настройка портов на Railway

## Проблема: Сервер запускается на порту 8080 вместо 3000

Railway автоматически устанавливает переменную окружения `PORT`, которая может отличаться от порта, указанного в настройках Networking.

## Решение

### Вариант 1: Явно установить PORT=3000 (РЕКОМЕНДУЕТСЯ)

1. Откройте Railway Dashboard → Backend сервис → Variables
2. Добавьте переменную окружения:
   ```
   PORT=3000
   ```
3. Убедитесь, что в настройках Networking порт также установлен на `3000`:
   - Railway Dashboard → Backend сервис → Settings → Networking
   - Порт должен быть `3000`

### Вариант 2: Использовать порт, который устанавливает Railway

Если Railway автоматически устанавливает `PORT=8080`, то:
1. Измените настройки Networking:
   - Railway Dashboard → Backend сервис → Settings → Networking
   - Установите порт на `8080`
2. Обновите переменную `VITE_API_URL` во фронтенде (если нужно)

## Важно

- **Backend сервис**: Порт должен совпадать в двух местах:
  1. Переменная окружения `PORT` (Variables)
  2. Настройки Networking → Port

- **Frontend сервис**: Nginx слушает на порту `80` (это правильно, Railway проксирует автоматически)

## Проверка

После установки `PORT=3000`:
1. Перезапустите бэкенд сервис
2. Проверьте логи: должно быть `🚀 Server running on port 3000`
3. Проверьте работу: `https://ambassadors-tma-production.up.railway.app/health`

