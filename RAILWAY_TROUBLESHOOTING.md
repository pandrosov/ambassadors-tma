# Troubleshooting Railway Deployment

## 🔍 Диагностика ошибок

### Проверка логов

В Railway Dashboard:
1. Откройте ваш сервис
2. Перейдите на вкладку "Deployments"
3. Выберите последний деплой
4. Откройте "View Logs"
5. Проверьте полные логи ошибки

### Частые проблемы и решения

#### 1. Ошибка запуска приложения

**Симптомы:**
- Сборка успешна, но приложение не запускается
- Ошибка "Cannot find module" или "Module not found"

**Решение:**
- Убедитесь, что Root Directory = `backend`
- Проверьте, что команда запуска правильная: `node dist/index.js`
- Убедитесь, что файл `dist/index.js` существует после сборки

#### 2. Ошибка подключения к базе данных

**Симптомы:**
- Ошибка "Can't reach database server"
- "Connection refused" или "Connection timeout"

**Решение:**
1. Создайте PostgreSQL базу данных в Railway:
   - В проекте нажмите "+ New"
   - Выберите "Database" → "Add PostgreSQL"
   
2. Добавьте переменную окружения:
   ```
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   ```
   (Railway автоматически подставит URL базы данных)

3. Примените миграции:
   - В Railway Dashboard откройте ваш сервис
   - Перейдите в "Deployments"
   - Нажмите "..." → "Run Command"
   - Выполните: `npx prisma migrate deploy`

#### 3. Отсутствуют переменные окружения

**Симптомы:**
- Ошибка "Environment variable not found"
- Приложение не может найти конфигурацию

**Решение:**
Добавьте все необходимые переменные в Railway Dashboard → Settings → Variables:

```env
# Database (добавляется автоматически при создании PostgreSQL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server
PORT=3000
NODE_ENV=production

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# JWT
JWT_SECRET=your_jwt_secret_here

# Google Sheets (опционально)
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
GOOGLE_SHEETS_CLIENT_EMAIL=your_client_email
GOOGLE_SHEETS_PRIVATE_KEY=your_private_key

# CORS (для фронтенда на Vercel)
FRONTEND_URL=https://your-app.vercel.app
```

#### 4. Ошибка "Cannot find module '@prisma/client'"

**Симптомы:**
- Prisma Client не найден при запуске

**Решение:**
- Убедитесь, что в `package.json` есть скрипт `postinstall: prisma generate`
- Railway должен автоматически выполнить его после `npm install`

#### 5. Порт не открыт

**Симптомы:**
- Приложение запускается, но недоступно извне

**Решение:**
- Railway автоматически открывает порт из переменной `PORT`
- Убедитесь, что приложение слушает на `process.env.PORT || 3000`
- Не используйте фиксированный порт (например, `3000`)

## 📋 Чеклист перед деплоем

- [ ] Root Directory установлен в `backend`
- [ ] PostgreSQL база данных создана
- [ ] Переменная `DATABASE_URL` настроена
- [ ] Переменная `TELEGRAM_BOT_TOKEN` добавлена
- [ ] Переменная `JWT_SECRET` добавлена
- [ ] Команда запуска: `node dist/index.js`
- [ ] Файл `dist/index.js` существует после сборки

## 🔧 Команды для диагностики

В Railway Dashboard → Deployments → "..." → "Run Command":

```bash
# Проверить структуру файлов
ls -la

# Проверить наличие dist
ls -la dist/

# Проверить переменные окружения
env | grep DATABASE_URL
env | grep TELEGRAM_BOT_TOKEN

# Проверить подключение к базе данных
npx prisma db pull

# Применить миграции
npx prisma migrate deploy

# Проверить, что приложение может запуститься
node dist/index.js
```

## 📞 Если ничего не помогает

1. Проверьте полные логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных создана и доступна
4. Убедитесь, что Root Directory = `backend`
5. Попробуйте пересобрать проект (Redeploy)

