# 🔧 Устранение проблем на Railway

## Проблема: 502 Bad Gateway на бэкенде

502 ошибка означает, что Railway не может подключиться к вашему приложению.

### Возможные причины:

1. **Бэкенд не запускается**
   - Проверьте логи в Railway Dashboard → Backend сервис → Logs
   - Ищите ошибки при запуске (например, ошибки Prisma, отсутствующие переменные окружения)

2. **Бэкенд падает после запуска**
   - Проверьте логи на наличие ошибок после строки "🚀 Server running on port..."
   - Проверьте, что все переменные окружения настроены правильно

3. **Неправильный порт**
   - Бэкенд должен слушать на порту из переменной `PORT` (Railway устанавливает автоматически)
   - Проверьте, что в коде используется `process.env.PORT || 3000`

4. **Отсутствующие переменные окружения**
   - Проверьте, что все обязательные переменные настроены (см. `RAILWAY_ENV_VARIABLES.md`)
   - Особенно важны: `DATABASE_URL`, `TELEGRAM_BOT_TOKEN`, `JWT_SECRET`

### Шаги для диагностики:

1. **Откройте логи бэкенда в Railway Dashboard**
   ```
   Backend сервис → Logs
   ```

2. **Проверьте, что бэкенд запускается:**
   - Должна быть строка: `🚀 Server running on port XXXX`
   - Если её нет - бэкенд не запускается

3. **Проверьте переменные окружения:**
   ```
   Backend сервис → Variables
   ```
   Убедитесь, что есть:
   - `DATABASE_URL` (должна быть подключена к PostgreSQL)
   - `TELEGRAM_BOT_TOKEN`
   - `JWT_SECRET`
   - `NODE_ENV=production`

4. **Проверьте подключение к базе данных:**
   - В логах не должно быть ошибок подключения к PostgreSQL
   - Убедитесь, что база данных создана и подключена к бэкенду

5. **Проверьте Prisma миграции:**
   - Если видите ошибки Prisma, возможно нужно запустить миграции
   - Railway должен автоматически запускать `prisma migrate deploy` при деплое

### Частые ошибки:

#### "Prisma Client not found"
- **Решение:** Убедитесь, что в Dockerfile правильно копируется Prisma Client из builder stage

#### "Cannot connect to database"
- **Решение:** Проверьте `DATABASE_URL` и убедитесь, что база данных подключена к сервису

#### "Port already in use"
- **Решение:** Убедитесь, что используется `process.env.PORT`, а не фиксированный порт

---

## Проблема: Nginx не запускается на фронтенде

### Решение:
✅ Исправлено - теперь используется фиксированный порт 80, Railway проксирует автоматически.

Если проблема сохраняется:
1. Проверьте логи фронтенда в Railway Dashboard
2. Убедитесь, что сборка прошла успешно
3. Проверьте, что `dist` директория создана после сборки

---

## Проверка после деплоя

### Backend:
1. Откройте URL бэкенда (например: `https://ambassadors-tma-production.up.railway.app`)
2. Должен вернуться ответ (не 502)
3. Проверьте `/health` endpoint (если есть)

### Frontend:
1. Откройте URL фронтенда
2. Страница должна загрузиться
3. Проверьте консоль браузера на ошибки

---

## Получение помощи

Если проблема не решается:
1. Скопируйте полные логи из Railway Dashboard
2. Проверьте все переменные окружения
3. Убедитесь, что база данных подключена и работает
