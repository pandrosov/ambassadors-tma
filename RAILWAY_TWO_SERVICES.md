# Создание двух сервисов в Railway (Backend + Frontend)

## 🎯 Цель

Создать два отдельных сервиса в одном Railway проекте:
- **Backend сервис** - для API
- **Frontend сервис** - для Telegram Mini App

## 📋 Пошаговая инструкция

### Шаг 1: Переименуйте текущий сервис в "backend"

1. В Railway Dashboard откройте ваш проект
2. Найдите сервис "web" (который сейчас упал)
3. Нажмите на него
4. Перейдите в **Settings**
5. Найдите поле **"Service Name"** или **"Name"**
6. Измените название на: `backend`
7. Сохраните изменения

### Шаг 2: Настройте Backend сервис

В настройках backend сервиса:

1. **Root Directory**: `.` (корень проекта) или оставьте пустым
2. **Build Command**: `cd backend && npm install && npm run build`
3. **Start Command**: `cd backend && node dist/index.js`

Или Railway должен автоматически использовать `railway.json` из корня.

### Шаг 3: Создайте Frontend сервис

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** (вверху справа)
3. Выберите **"GitHub Repo"**
4. Выберите репозиторий `pandrosov/ambassadors-tma`
5. После создания сервиса:
   - **Service Name**: `frontend`
   - **Root Directory**: `frontend`
   - Railway автоматически определит Vite проект

### Шаг 4: Настройте переменные окружения

#### В Backend сервисе:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
PORT=3000
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен
JWT_SECRET=ваш_секрет
FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
```

#### В Frontend сервисе:

```
VITE_API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}
PORT=3001
```

## 🔧 Альтернативный способ через Railway CLI

Если веб-интерфейс не позволяет создать второй сервис:

```bash
# Войдите в Railway
railway login

# Создайте новый сервис для фронтенда
railway init --service frontend

# Установите root directory
railway link --service frontend
cd frontend
railway up
```

## 📝 Структура после настройки

```
Railway проект
├── Postgres (база данных)
│
├── Backend сервис
│   ├── Root Directory: . (корень)
│   ├── Build: cd backend && npm install && npm run build
│   ├── Start: cd backend && node dist/index.js
│   └── URL: https://backend-production-xxxx.up.railway.app
│
└── Frontend сервис
    ├── Root Directory: frontend
    ├── Build: npm install && npm run build
    ├── Start: serve -s dist -l $PORT
    └── URL: https://frontend-production-xxxx.up.railway.app
```

## ⚠️ Важно

1. **Root Directory** должен быть разным для каждого сервиса:
   - Backend: `.` (корень) или пусто
   - Frontend: `frontend`

2. **Переменные окружения** настраиваются отдельно для каждого сервиса

3. **Связь между сервисами** через переменные:
   - Frontend использует: `VITE_API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}`
   - Backend использует: `FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}`

## 🔍 Проверка

После создания обоих сервисов вы должны увидеть:
- **Backend** сервис (зеленый статус)
- **Frontend** сервис (зеленый статус)
- **Postgres** база данных

Все три элемента в одном проекте!

