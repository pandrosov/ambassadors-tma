# 🚀 Инструкция по запуску серверов

## Быстрый запуск

```bash
./start.sh
```

Или вручную:

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## 📍 Адреса серверов

- **Backend API**: http://localhost:3000
- **Frontend (HTTPS)**: https://localhost:5173
- **Health Check**: http://localhost:3000/health

## 🔒 HTTPS для Telegram Mini Apps

Frontend настроен на работу через HTTPS с самоподписанным сертификатом.

**Важно для Telegram:**
- Telegram Mini Apps требуют HTTPS
- Для локальной разработки используйте `https://localhost:5173`
- При первом открытии браузер покажет предупреждение о недоверенном сертификате - это нормально для разработки
- Для продакшена используйте реальный SSL сертификат

## 📝 Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/botfather)
2. Выполните `/newapp`
3. Выберите вашего бота
4. Укажите URL: `https://localhost:5173` (для локальной разработки)

**Для доступа извне (если нужно):**
- Используйте ngrok: `ngrok http 5173`
- Или другой туннель для HTTPS

## 🛑 Остановка серверов

```bash
# Найти процессы
ps aux | grep -E "(tsx|vite)" | grep -v grep

# Остановить по PID
kill <PID>

# Или остановить все
pkill -f "tsx watch"
pkill -f "vite"
```

## 📊 Просмотр логов

```bash
# Backend
tail -f /tmp/backend.log

# Frontend
tail -f /tmp/frontend.log
```

## ✅ Проверка работы

1. **Backend**: Откройте http://localhost:3000/health
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend**: Откройте https://localhost:5173
   - Должно открыться приложение
   - Браузер может показать предупреждение о сертификате - нажмите "Продолжить"

3. **Telegram**: Добавьте Mini App через BotFather
   - URL: `https://localhost:5173`
   - Откройте бота и нажмите на кнопку Mini App

