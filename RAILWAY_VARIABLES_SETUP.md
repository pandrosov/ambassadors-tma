# Настройка переменных окружения в Railway

## 🔍 Разница между Shared Variables и Service Variables

### Shared Variables (Общие переменные)
- Доступны во **всех сервисах** проекта
- Используются через синтаксис: `${{Shared.VARIABLE_NAME}}`
- Удобны для значений, которые нужны в нескольких сервисах

### Service Variables (Переменные сервиса)
- Доступны **только в конкретном сервисе**
- Используются напрямую: `process.env.VARIABLE_NAME`
- Это то, что нужно для вашего backend сервиса

## ✅ Правильная настройка для Backend сервиса

### Шаг 1: Откройте настройки вашего сервиса

1. В Railway Dashboard откройте ваш проект
2. Найдите ваш **backend сервис** (не проект целиком!)
3. Нажмите на сервис, чтобы открыть его настройки
4. Перейдите на вкладку **"Variables"**

### Шаг 2: Добавьте переменные для сервиса

В разделе **"Variables"** (не "Shared Variables"!) добавьте:

#### Обязательные переменные:

```env
# Database (если PostgreSQL уже создан, Railway добавит автоматически)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server
PORT=3000
NODE_ENV=production

# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=ваш_секретный_ключ_здесь_минимум_32_символа
```

#### Опциональные переменные:

```env
# Google Sheets (если используете)
GOOGLE_SHEETS_SPREADSHEET_ID=ваш_spreadsheet_id
GOOGLE_SHEETS_CLIENT_EMAIL=ваш_client_email
GOOGLE_SHEETS_PRIVATE_KEY=ваш_private_key

# CORS (для фронтенда на Vercel)
FRONTEND_URL=https://your-app.vercel.app
```

### Шаг 3: Использование Shared Variables

Если вы хотите использовать Shared Variables, добавьте их в Service Variables так:

```env
# В Service Variables вашего backend сервиса:
MY_VARIABLE=${{Shared.MY_SHARED_VARIABLE}}
```

Но для большинства случаев лучше добавлять переменные напрямую в Service Variables.

## 📋 Чеклист

- [ ] Открыт **сервис** (не проект)
- [ ] Переменные добавлены в раздел **"Variables"** (не "Shared Variables")
- [ ] `DATABASE_URL` добавлена (или создана PostgreSQL база)
- [ ] `TELEGRAM_BOT_TOKEN` добавлен
- [ ] `JWT_SECRET` добавлен
- [ ] `PORT=3000` добавлен
- [ ] `NODE_ENV=production` добавлен

## 🔧 Где найти настройки

1. **Railway Dashboard** → Ваш проект
2. **Ваш сервис** (например, "backend" или название вашего сервиса)
3. Вкладка **"Variables"** (вверху или в боковом меню)
4. Кнопка **"+ New Variable"** или **"Add Variable"**

## ⚠️ Важно

- Переменные должны быть добавлены в **Service Variables**, а не только в Shared Variables
- После добавления переменных Railway автоматически перезапустит сервис
- Проверьте логи после перезапуска, чтобы убедиться, что переменные загружены

