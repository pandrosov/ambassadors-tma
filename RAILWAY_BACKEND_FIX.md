# 🔧 Исправление проблемы с Backend Dockerfile на Railway

## Проблема
Ошибка: `"/backend": not found` при сборке на Railway

## Решение 1: Изменить Root Directory (РЕКОМЕНДУЕТСЯ)

Если текущий подход не работает, измените настройки Railway:

1. Откройте **Backend сервис** → **Settings**
2. Найдите **"Root Directory"**
3. Измените с `backend` на `.` (точка - корень проекта)
4. Найдите **"Dockerfile Path"** или **"Dockerfile"**
5. Укажите: `backend/Dockerfile`
6. Сохраните изменения

Railway будет использовать корень проекта как build context, но Dockerfile будет из `backend/Dockerfile`.

---

## Решение 2: Использовать корневой Dockerfile

Создайте `Dockerfile` в корне проекта специально для backend:

1. Скопируйте содержимое `backend/Dockerfile` в корневой `Dockerfile`
2. В Railway установите:
   - **Root Directory:** `.`
   - **Dockerfile:** `Dockerfile` (по умолчанию)

---

## Текущая настройка

Сейчас используется:
- **Root Directory:** `backend`
- **Dockerfile:** `backend/Dockerfile` (автоматически)

Если это не работает, используйте Решение 1.

