# Деплой Frontend на Railway

## ✅ Да, можно деплоить фронтенд на Railway!

Railway поддерживает статические сайты и фронтенд приложения. Это даже удобнее - все в одном месте!

## 🚀 Варианты деплоя

### Вариант 1: Отдельный сервис для Frontend (рекомендуется)

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** → **"GitHub Repo"**
3. Выберите тот же репозиторий `pandrosov/ambassadors-tma`
4. В настройках сервиса:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: Railway автоматически определит статический сайт
   - Или используйте: `npx serve -s dist -l $PORT`

5. Добавьте переменные окружения:
   ```
   VITE_API_URL=https://your-backend-service.up.railway.app
   PORT=3001
   ```

6. Railway автоматически определит, что это статический сайт и настроит сервирование

### Вариант 2: Использование Railway Static Site

Railway автоматически определяет статические сайты и использует встроенный сервер для их раздачи.

## 📋 Настройка

### 1. Создайте отдельный сервис для Frontend

В Railway Dashboard:
- **"+ New"** → **"GitHub Repo"**
- Выберите `pandrosov/ambassadors-tma`
- **Root Directory**: `frontend`

### 2. Настройте переменные окружения

В настройках frontend сервиса добавьте:
```
VITE_API_URL=https://your-backend-service.up.railway.app
```

### 3. Railway автоматически определит Vite проект

Railway определит `vite.config.ts` и автоматически настроит сборку и деплой.

## 🔧 Альтернатива: Использование nginx или serve

Если Railway не определит автоматически, можно использовать:

### Способ 1: serve (простой)

Создайте `frontend/package.json` с дополнительным скриптом:
```json
{
  "scripts": {
    "start": "serve -s dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
```

### Способ 2: nginx (для production)

Создайте `frontend/nginx.conf`:
```nginx
server {
    listen $PORT;
    root /app/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## ✅ Преимущества деплоя на Railway

1. **Все в одном месте** - backend и frontend в одном проекте
2. **Проще управление** - один dashboard для всего
3. **Автоматические переменные** - можно использовать `${{ServiceName.VARIABLE}}`
4. **Единый домен** - можно настроить поддомены
5. **Проще отладка** - все логи в одном месте

## 📝 Структура проекта в Railway

```
Ваш проект (Railway)
├── Backend сервис
│   ├── Root Directory: . (корень)
│   ├── Build: cd backend && npm install && npm run build
│   └── Start: cd backend && node dist/index.js
│
└── Frontend сервис
    ├── Root Directory: frontend
    ├── Build: npm install && npm run build
    └── Start: serve -s dist -l $PORT
```

## 🔗 Настройка связи между сервисами

### В Frontend сервисе:

Добавьте переменную окружения:
```
VITE_API_URL=${{BackendService.RAILWAY_PUBLIC_DOMAIN}}
```

Или используйте прямое имя сервиса:
```
VITE_API_URL=https://your-backend-service.up.railway.app
```

### В Backend сервисе:

Добавьте переменную для CORS:
```
FRONTEND_URL=${{FrontendService.RAILWAY_PUBLIC_DOMAIN}}
```

Или прямое имя:
```
FRONTEND_URL=https://your-frontend-service.up.railway.app
```

## 🎯 Итоговая структура

После настройки у вас будет:
- **Backend**: `https://backend-service.up.railway.app`
- **Frontend**: `https://frontend-service.up.railway.app`
- Оба в одном Railway проекте!

