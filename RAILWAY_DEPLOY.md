# Пошаговая инструкция по деплою на Railway

## 🚀 Быстрый старт

### Шаг 1: Подготовка репозитория

1. Убедитесь, что весь код закоммичен в GitHub:
   ```bash
   git add .
   git commit -m "Prepare for Railway deployment"
   git push origin main
   ```

2. Проверьте, что файлы на месте:
   - `railway.json` ✅
   - `Procfile` ✅
   - `backend/package.json` с правильными скриптами ✅

### Шаг 2: Регистрация на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Выберите "Login with GitHub"
4. Авторизуйтесь через GitHub

### Шаг 3: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий `ambassadors_tma`
4. Railway автоматически определит проект

### Шаг 4: Настройка Backend сервиса

1. Railway создаст сервис автоматически
2. Нажмите на сервис для настройки
3. В разделе "Settings" → "Service Name" назовите его `backend`

#### Настройка переменных окружения:

В разделе "Variables" добавьте следующие переменные:

```env
# Database (будет добавлена автоматически при создании PostgreSQL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server
PORT=3000
NODE_ENV=production

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
FRONTEND_URL=https://your-frontend-url.railway.app

# JWT
JWT_SECRET=your_very_long_and_random_secret_key_here

# Google Sheets (опционально)
GOOGLE_SHEETS_CLIENT_EMAIL=your_email@example.com
GOOGLE_SHEETS_PRIVATE_KEY=your_private_key
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
```

**Важно:**
- `DATABASE_URL` будет автоматически добавлен при создании PostgreSQL
- `FRONTEND_URL` обновите после деплоя frontend
- `JWT_SECRET` сгенерируйте случайную строку (минимум 32 символа)

### Шаг 5: Создание PostgreSQL базы данных

1. В проекте нажмите "New" → "Database" → "PostgreSQL"
2. Railway автоматически создаст базу данных
3. Переменная `DATABASE_URL` будет автоматически добавлена в ваш сервис
4. Скопируйте `DATABASE_URL` из переменных окружения

### Шаг 6: Настройка деплоя Backend

1. В настройках сервиса перейдите в "Deploy"
2. Убедитесь, что:
   - **Root Directory:** `backend` (или оставьте пустым, если Railway определит автоматически)
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`

3. Railway автоматически определит настройки из `railway.json` и `package.json`

### Шаг 7: Применение миграций базы данных

После первого деплоя нужно применить миграции:

1. В Railway откройте ваш сервис
2. Перейдите в "Deployments" → выберите последний деплой
3. Откройте "View Logs"
4. Найдите URL вашего приложения (например, `https://your-app.up.railway.app`)

**Вариант 1: Через Railway CLI**
```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Подключитесь к проекту
railway link

# Примените миграции
cd backend
railway run npx prisma db push
```

**Вариант 2: Через локальную машину**
```bash
# Установите DATABASE_URL из Railway в локальный .env
# Скопируйте DATABASE_URL из Railway Variables

# Примените миграции
cd backend
npx prisma db push
```

**Вариант 3: Через Railway Deploy Logs (если добавили в package.json)**
Railway автоматически выполнит `postinstall` который запустит `prisma generate`, но для миграций нужно выполнить вручную.

### Шаг 8: Создание Frontend сервиса

1. В проекте Railway нажмите "New" → "GitHub Repo"
2. Выберите тот же репозиторий
3. В настройках сервиса:
   - **Root Directory:** `frontend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx serve -s dist -l 3000`

4. Добавьте переменные окружения:
   ```env
   VITE_API_URL=https://your-backend-url.railway.app
   ```

5. Обновите `FRONTEND_URL` в backend сервисе на URL frontend сервиса

### Шаг 9: Настройка домена (опционально)

1. В настройках сервиса перейдите в "Settings" → "Networking"
2. Нажмите "Generate Domain"
3. Railway создаст домен типа `your-app.up.railway.app`
4. Скопируйте URL и обновите `FRONTEND_URL` в backend

### Шаг 10: Проверка деплоя

1. **Backend:**
   ```bash
   curl https://your-backend-url.railway.app/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend:**
   Откройте URL frontend в браузере

3. **База данных:**
   ```bash
   # Через Railway CLI
   railway run npx prisma studio
   ```

### Шаг 11: Настройка Telegram Bot

1. Обновите webhook URL в Telegram:
   ```bash
   curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -d "url=https://your-backend-url.railway.app/webhook/telegram"
   ```

2. Обновите URL Mini App в BotFather:
   ```
   /newapp
   [выберите вашего бота]
   [название приложения]
   https://your-frontend-url.railway.app
   ```

## 🔧 Решение проблем

### Проблема: Приложение не запускается

1. Проверьте логи в Railway: "Deployments" → выберите деплой → "View Logs"
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `DATABASE_URL` правильный
4. Убедитесь, что `PORT` установлен (Railway использует переменную `PORT`)

### Проблема: База данных не подключается

1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что PostgreSQL сервис запущен
3. Проверьте логи подключения в логах приложения

### Проблема: Prisma миграции не применяются

1. Выполните вручную через Railway CLI:
   ```bash
   railway run npx prisma db push
   ```

2. Или добавьте в `package.json` скрипт:
   ```json
   "railway:deploy": "prisma generate && prisma db push && npm start"
   ```

### Проблема: Frontend не может подключиться к Backend

1. Проверьте `VITE_API_URL` в frontend переменных
2. Убедитесь, что CORS настроен правильно в backend
3. Проверьте, что backend URL правильный и доступен

## 📊 Мониторинг

### Просмотр логов

1. В Railway откройте сервис
2. Перейдите в "Deployments"
3. Выберите деплой → "View Logs"

### Метрики

Railway показывает:
- CPU использование
- Memory использование
- Network трафик
- Request count

## 💰 Стоимость

Railway предоставляет:
- **$5 бесплатных кредитов в месяц**
- После исчерпания кредитов требуется оплата
- Примерная стоимость: ~$5-20/месяц для небольшого проекта

## 🔄 Обновление приложения

Railway автоматически деплоит при каждом push в GitHub:
1. Сделайте изменения в коде
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Railway автоматически запустит новый деплой

## 📝 Полезные команды Railway CLI

```bash
# Войти в Railway
railway login

# Подключиться к проекту
railway link

# Запустить команду в окружении Railway
railway run <command>

# Просмотреть логи
railway logs

# Открыть базу данных
railway run npx prisma studio

# Применить миграции
railway run npx prisma db push
```

## ✅ Чеклист деплоя

- [ ] Код закоммичен в GitHub
- [ ] Создан проект на Railway
- [ ] Создан Backend сервис
- [ ] Создана PostgreSQL база данных
- [ ] Настроены все переменные окружения
- [ ] Применены миграции базы данных
- [ ] Создан Frontend сервис
- [ ] Настроены домены
- [ ] Проверена работа backend (`/health`)
- [ ] Проверена работа frontend
- [ ] Настроен Telegram webhook
- [ ] Обновлен URL Mini App в BotFather

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет доступно по адресам:
- **Backend:** `https://your-backend.up.railway.app`
- **Frontend:** `https://your-frontend.up.railway.app`
- **Database:** Доступна через Railway

