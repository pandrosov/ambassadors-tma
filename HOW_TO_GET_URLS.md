# Как узнать URL бэкенда и фронтенда

## 🔵 Backend URL (Railway)

### Способ 1: Через Dashboard (самый простой)

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект
3. Откройте ваш **backend сервис**
4. Перейдите на вкладку **"Settings"**
5. Найдите раздел **"Networking"** или **"Domains"**
6. Там будет показан ваш URL, например:
   ```
   https://your-app-name.up.railway.app
   ```

### Способ 2: Через вкладку "Deployments"

1. Откройте ваш backend сервис
2. Перейдите на вкладку **"Deployments"**
3. Выберите последний успешный деплой
4. В логах или информации о деплое будет указан URL

### Способ 3: Через переменные окружения

Railway автоматически создает переменную `RAILWAY_PUBLIC_DOMAIN`, которая содержит ваш URL.

Чтобы увидеть её:
1. Откройте ваш сервис → Settings → Variables
2. Найдите `RAILWAY_PUBLIC_DOMAIN`

### Способ 4: Через Railway CLI

```bash
# Если вы авторизованы через CLI
railway status

# Или
railway domain
```

## 🟢 Frontend URL (Vercel)

### Способ 1: Через Dashboard (самый простой)

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. После деплоя URL будет показан:
   - В карточке проекта (вверху)
   - В списке деплоев (после каждого деплоя)
   
   Пример URL:
   ```
   https://your-app-name.vercel.app
   ```

### Способ 2: Через вкладку "Deployments"

1. Откройте ваш проект в Vercel
2. Перейдите на вкладку **"Deployments"**
3. Выберите последний деплой
4. URL будет показан рядом с деплоем

### Способ 3: Через Vercel CLI

```bash
# Если вы авторизованы через CLI
vercel ls

# Или после деплоя
vercel
```

## 🔗 Настройка связи между Frontend и Backend

### После получения URL:

1. **Backend URL** (Railway):
   ```
   https://your-backend.up.railway.app
   ```

2. **Frontend URL** (Vercel):
   ```
   https://your-frontend.vercel.app
   ```

### Настройка Frontend для работы с Backend:

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   ```
   VITE_API_URL=https://your-backend.up.railway.app
   ```
4. Пересоберите проект (Redeploy)

### Настройка CORS в Backend:

1. В Railway Dashboard откройте ваш backend сервис
2. Перейдите в **Settings** → **Variables**
3. Добавьте переменную:
   ```
   FRONTEND_URL=https://your-frontend.vercel.app
   ```
4. Убедитесь, что в коде backend настроен CORS:
   ```typescript
   app.use(cors({
     origin: process.env.FRONTEND_URL || 'https://your-frontend.vercel.app',
     credentials: true
   }));
   ```

## 📋 Чеклист

- [ ] Backend URL получен из Railway Dashboard
- [ ] Frontend URL получен из Vercel Dashboard
- [ ] `VITE_API_URL` настроен в Vercel (указывает на Railway backend)
- [ ] `FRONTEND_URL` настроен в Railway (для CORS)
- [ ] CORS настроен в backend коде
- [ ] Web App URL обновлен в Telegram боте через @BotFather

## 🔍 Где найти URL в интерфейсе

### Railway:
```
Проект → Сервис → Settings → Networking/Domains
или
Проект → Сервис → Deployments → Последний деплой
```

### Vercel:
```
Проект → Карточка проекта (вверху)
или
Проект → Deployments → Последний деплой
```

## ⚠️ Важно

- Railway URL может измениться при пересоздании сервиса
- Для постоянного URL можно настроить кастомный домен в Railway
- Vercel предоставляет постоянный URL вида `project-name.vercel.app`
- После изменения URL нужно обновить переменные окружения и пересобрать проекты

