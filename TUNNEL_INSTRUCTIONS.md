# 🔗 Инструкция по созданию публичного HTTPS туннеля

## Проблема с ngrok

Ngrok блокирует ваш IP адрес. Используем **Cloudflare Tunnel** (cloudflared) - бесплатный и без ограничений.

## ✅ Быстрый способ

### Вариант 1: Автоматический скрипт

```bash
./start-with-cloudflare.sh
```

Скрипт запустит все серверы и создаст туннель автоматически.

### Вариант 2: Ручной запуск

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

**Терминал 3 - Cloudflare Tunnel:**
```bash
cloudflared tunnel --url https://localhost:5173
```

Через несколько секунд вы увидите строку типа:
```
https://abc123-def456.trycloudflare.com
```

Это ваш публичный HTTPS URL!

## 📱 Использование в Telegram

1. Скопируйте публичный URL (например: `https://abc123-def456.trycloudflare.com`)
2. Откройте [@BotFather](https://t.me/botfather)
3. Выполните `/newapp`
4. Выберите вашего бота
5. Вставьте публичный URL

## 🔍 Получить URL после запуска

Если туннель уже запущен, выполните:

```bash
./get-tunnel-url.sh
```

Или проверьте логи Cloudflare Tunnel - там будет URL.

## ⚠️ Важные замечания

1. **URL меняется** при каждом перезапуске cloudflared
   - После каждого перезапуска нужно обновлять URL в BotFather
   - Для постоянного URL нужна регистрация в Cloudflare (бесплатно)

2. **Туннель работает пока запущен**
   - Не закрывайте терминал с cloudflared
   - Или запускайте в фоне: `cloudflared tunnel --url https://localhost:5173 &`

3. **Для продакшена** используйте:
   - Реальный домен с SSL сертификатом
   - Или зарегистрируйтесь в Cloudflare для постоянного домена

## 🛑 Остановка

```bash
# Остановить все
pkill cloudflared
pkill -f "vite"
pkill -f "tsx watch"
```

## ✅ Проверка работы

1. Откройте публичный URL в браузере - должно открыться ваше приложение
2. Откройте бота в Telegram и нажмите на кнопку Mini App
3. Приложение должно открыться внутри Telegram

