# 🔗 Настройка публичного HTTPS туннеля для Telegram Mini Apps

## Проблема

Telegram Mini Apps **не могут** обращаться к `localhost` или `127.0.0.1`, так как это локальные адреса, недоступные с устройств пользователей.

## Решение: Ngrok туннель

Используем ngrok для создания публичного HTTPS URL, который проксирует запросы на ваш локальный сервер.

## Быстрый запуск с туннелем

```bash
./start-with-tunnel.sh
```

Этот скрипт:
1. Запустит backend на `http://localhost:3000`
2. Запустит frontend на `https://localhost:5173`
3. Создаст ngrok туннель и покажет публичный URL

## Ручная настройка

### 1. Запустите серверы

```bash
# Терминал 1 - Backend
cd backend
npm run dev

# Терминал 2 - Frontend
cd frontend
npm run dev
```

### 2. Создайте ngrok туннель

```bash
# Терминал 3 - Ngrok
ngrok http 5173
```

### 3. Получите публичный URL

Откройте http://localhost:4040 в браузере или выполните:

```bash
curl http://localhost:4040/api/tunnels | python3 -m json.tool
```

Найдите поле `public_url` - это ваш публичный HTTPS URL.

Пример: `https://abc123.ngrok-free.app`

## Использование в Telegram

1. Откройте [@BotFather](https://t.me/botfather)
2. Выполните `/newapp`
3. Выберите вашего бота
4. Укажите публичный ngrok URL (например: `https://abc123.ngrok-free.app`)

## ⚠️ Важные замечания

1. **Ngrok URL меняется** при каждом перезапуске (на бесплатном плане)
   - После каждого перезапуска нужно обновлять URL в BotFather
   - Или используйте ngrok с фиксированным доменом (платный план)

2. **Предупреждение ngrok** - при первом открытии ngrok покажет предупреждение
   - Это нормально для бесплатного плана
   - Пользователь должен нажать "Visit Site"

3. **Для продакшена** используйте:
   - Реальный домен с SSL сертификатом
   - Или платный ngrok с фиксированным доменом

## Альтернативы ngrok

- **Cloudflare Tunnel** (cloudflared) - бесплатный, без ограничений
- **LocalTunnel** - бесплатный, простой
- **Serveo** - бесплатный SSH туннель

## Проверка работы

1. Откройте публичный ngrok URL в браузере
2. Должно открыться ваше приложение
3. Откройте бота в Telegram и нажмите на кнопку Mini App
4. Приложение должно открыться внутри Telegram

## Остановка

```bash
# Остановить все процессы
pkill -f "tsx watch"
pkill -f "vite"
pkill ngrok
```

Или используйте сохраненные PIDs:

```bash
kill $(cat /tmp/ambassadors_pids.txt)
```

