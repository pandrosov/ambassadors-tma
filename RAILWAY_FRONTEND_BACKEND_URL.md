# 🔗 Настройка связи Frontend ↔ Backend на Railway

## Проблема: Frontend не может подключиться к Backend

Если фронтенд показывает ошибку `Network Error` или `ERR_CONNECTION_REFUSED` при попытке подключиться к `localhost:3000`, это означает, что фронтенд не знает URL бэкенда.

## ✅ Решение 1: Установить переменную окружения (РЕКОМЕНДУЕТСЯ)

### Шаги:

1. **Узнайте URL вашего бэкенда:**
   - Откройте Railway Dashboard
   - Выберите ваш **Backend** сервис
   - Перейдите в **Settings** → **Domains**
   - Скопируйте URL (например: `https://ambassadors-tma-production.up.railway.app`)

2. **Установите переменную окружения для Frontend:**
   - Откройте Railway Dashboard
   - Выберите ваш **Frontend** сервис
   - Перейдите в **Variables**
   - Добавьте новую переменную:
     ```
     Name: VITE_API_URL
     Value: https://ambassadors-tma-production.up.railway.app
     ```
   - ⚠️ **ВАЖНО:** Замените `https://ambassadors-tma-production.up.railway.app` на ваш реальный URL бэкенда!

3. **Перезапустите Frontend сервис:**
   - Railway автоматически пересоберет фронтенд с новой переменной окружения
   - Дождитесь завершения деплоя

## ✅ Решение 2: Автоматическое определение (работает, но менее надежно)

Код фронтенда теперь автоматически пытается определить URL бэкенда на Railway:

1. Если домен фронтенда содержит `railway.app` или `up.railway.app`
2. Пытается заменить `frontend` на `backend` в домене
3. Если не сработало, использует fallback: `https://ambassadors-tma-production.up.railway.app`

**Примеры:**
- Фронтенд: `amb-frontend-production.up.railway.app` → Бэкенд: `amb-backend-production.up.railway.app`
- Фронтенд: `ambassadors-frontend.railway.app` → Бэкенд: `ambassadors-backend.railway.app`

⚠️ **Проблема:** Если ваши сервисы называются по-другому, автоматическое определение может не сработать.

## 🔍 Как проверить, что работает:

1. Откройте фронтенд в браузере
2. Откройте консоль разработчика (F12)
3. Найдите сообщение: `API URL configured: https://...`
4. Убедитесь, что URL указывает на ваш бэкенд, а не на `localhost:3000`

## 📝 Примеры правильных URL:

```
✅ Правильно:
VITE_API_URL=https://ambassadors-tma-production.up.railway.app
VITE_API_URL=https://your-backend-service.up.railway.app

❌ Неправильно:
VITE_API_URL=http://localhost:3000
VITE_API_URL=https://localhost:3000
```

## 🚨 Если проблема сохраняется:

1. Проверьте, что бэкенд действительно работает:
   - Откройте URL бэкенда в браузере
   - Должен быть ответ (например, `{"message":"API is running"}` или ошибка CORS)

2. Проверьте CORS настройки бэкенда:
   - Убедитесь, что `FRONTEND_URL` в бэкенде указывает на URL фронтенда
   - Проверьте логи бэкенда на наличие ошибок CORS

3. Проверьте логи фронтенда:
   - Railway Dashboard → Frontend сервис → Deployments → View Logs
   - Ищите ошибки сборки или запуска

4. Проверьте переменные окружения:
   - Railway Dashboard → Frontend сервис → Variables
   - Убедитесь, что `VITE_API_URL` установлена и содержит правильный URL

