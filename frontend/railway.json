{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd $RAILWAY_WORKSPACE_ROOT && npm install --workspaces && cd frontend && npm run build"
  },
  "deploy": {
    "startCommand": "cd $RAILWAY_WORKSPACE_ROOT/frontend && npx serve -s dist -l $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

