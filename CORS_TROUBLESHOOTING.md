# 🔧 Troubleshooting CORS на Railway

## Текущая ситуация:

✅ **CORS работает через curl**: Заголовки присутствуют в ответах
❌ **CORS не работает в браузере**: Ошибка "No 'Access-Control-Allow-Origin' header"

## Возможные причины:

### 1. Кеш браузера
Браузер может кешировать старые CORS ответы.

**Решение:**
- Очистите кеш браузера полностью
- Используйте режим инкогнито
- Откройте DevTools → Network → поставьте галочку "Disable cache"

### 2. Railway Edge проксирование
Railway Edge может по-разному обрабатывать запросы от браузера и curl.

**Проверка:**
```bash
# Проверьте логи бэкенда
railway logs --service amb-backend | grep CORS
```

### 3. Порядок middleware
CORS middleware должен быть ПЕРВЫМ.

**Проверено:** ✅ CORS middleware применен первым

### 4. Браузер отправляет другие заголовки
Браузер может отправлять дополнительные заголовки, которые не разрешены.

**Проверка:**
- Откройте DevTools → Network
- Найдите запрос с ошибкой CORS
- Проверьте заголовки запроса (Request Headers)
- Проверьте заголовки ответа (Response Headers)

## Диагностика:

### Шаг 1: Проверьте логи бэкенда

```bash
railway logs --service amb-backend | tail -100
```

Ищите:
- `[REQUEST] OPTIONS` - должны появляться для каждого preflight запроса
- `[CORS DEBUG]` - информация о CORS заголовках
- Ошибки или исключения

### Шаг 2: Проверьте заголовки в браузере

1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Найдите запрос с ошибкой CORS
4. Откройте детали запроса
5. Проверьте:
   - **Request Headers** → `Origin` должен быть `https://amb-frontend-production.up.railway.app`
   - **Response Headers** → должны быть заголовки `Access-Control-Allow-*`

### Шаг 3: Проверьте переменные окружения

```bash
railway variables --service amb-backend | grep FRONTEND_URL
```

Должно быть: `FRONTEND_URL=https://amb-frontend-production.up.railway.app`

### Шаг 4: Проверьте через curl

```bash
curl -X OPTIONS https://ambassadors-tma-production.up.railway.app/api/users/me \
  -H "Origin: https://amb-frontend-production.up.railway.app" \
  -H "Access-Control-Request-Method: GET" \
  -v
```

Должны быть заголовки `Access-Control-Allow-*`

## Если проблема сохраняется:

### Временное решение: Разрешить все origins (ТОЛЬКО ДЛЯ ОТЛАДКИ)

```typescript
// В backend/src/index.ts временно замените:
origin: true, // Разрешает все origins
```

⚠️ **ВНИМАНИЕ:** Это небезопасно для production! Используйте только для отладки.

### Альтернативное решение: Использовать Railway Private Networking

Если сервисы находятся в одном проекте Railway, можно использовать private networking вместо CORS.

## Проверка после исправлений:

1. Дождитесь завершения деплоя бэкенда
2. Очистите кеш браузера полностью
3. Откройте приложение в режиме инкогнито
4. Проверьте консоль браузера - ошибки CORS должны исчезнуть
5. Проверьте логи бэкенда - должны быть сообщения `[CORS DEBUG]`

