# 🔗 Стабильный URL для разработки

## Проблема
Cloudflare Tunnel генерирует случайный URL при каждом запуске (например: `https://buddy-eleven-bradford-holly.trycloudflare.com`), что неудобно для разработки.

## Решения

### 1. ✅ Использовать один туннель долго (самый простой)
**Как работает:**
- Запустите туннель один раз: `./restart-cloudflare.sh`
- URL будет работать, пока туннель не остановится
- Если не перезапускать туннель, URL останется прежним

**Команда:**
```bash
./restart-cloudflare.sh
```

**Сохраните URL:**
После запуска скопируйте URL из вывода и используйте его в Telegram Bot.

### 2. 🔐 Cloudflare Tunnel с именованным туннелем (рекомендуется для продакшена)
**Требования:**
- Аккаунт Cloudflare (бесплатный)
- Cloudflare Tunnel установлен

**Шаги:**
```bash
# 1. Авторизуйтесь в Cloudflare
cloudflared tunnel login

# 2. Создайте именованный туннель
cloudflared tunnel create ambassadors-tma

# 3. Настройте конфигурацию
mkdir -p ~/.cloudflared
cat > ~/.cloudflared/config.yml << EOF
tunnel: ambassadors-tma
credentials-file: ~/.cloudflared/[TUNNEL-ID].json

ingress:
  - hostname: ambassadors-tma.yourdomain.com  # Ваш домен
    service: http://localhost:5173
  - service: http_status:404
EOF

# 4. Запустите туннель
cloudflared tunnel run ambassadors-tma
```

**Преимущества:**
- Постоянный URL: `https://ambassadors-tma.yourdomain.com`
- Работает даже после перезапуска
- Можно использовать свой домен

### 3. 🌐 ngrok с зарезервированным доменом (платно)
**Требования:**
- Аккаунт ngrok (платный план от $8/месяц)
- Зарезервированный домен

**Шаги:**
```bash
# 1. Установите ngrok
brew install ngrok

# 2. Авторизуйтесь
ngrok config add-authtoken YOUR_TOKEN

# 3. Запустите с зарезервированным доменом
ngrok http 5173 --domain=your-reserved-domain.ngrok.io
```

**Преимущества:**
- Постоянный URL
- Простая настройка

### 4. 💻 Локальный туннель с постоянным доменом (бесплатно)
**Используйте локальный туннель:**
```bash
# Установите localtunnel
npm install -g localtunnel

# Запустите с субдоменом
lt --port 5173 --subdomain ambassadors-tma
```

**URL будет:** `https://ambassadors-tma.loca.lt`

**Ограничения:**
- Требует установки Node.js
- Может быть менее стабильным

## 📝 Рекомендация для разработки

**Для быстрого старта:**
1. Используйте вариант 1 (один туннель долго)
2. Сохраните URL в переменную окружения или файл
3. Используйте этот URL в Telegram Bot

**Скрипт для сохранения URL:**
```bash
#!/bin/bash
# save-url.sh
URL=$(./restart-cloudflare.sh | grep "Публичный URL" | awk '{print $3}')
echo "FRONTEND_URL=$URL" > .env.tunnel
echo "✅ URL сохранен: $URL"
```

**Для продакшена:**
- Используйте вариант 2 (Cloudflare Tunnel с именованным туннелем)
- Или вариант 3 (ngrok с зарезервированным доменом)

## 🔄 Текущий URL

После запуска `./restart-cloudflare.sh` URL будет выведен в консоль.
Сохраните его и используйте в Telegram Bot Settings.

