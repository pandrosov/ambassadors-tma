# Инструкция по запуску проекта

## ✅ Что уже настроено:

1. ✅ База данных PostgreSQL создана: `ambassadors_tma`
2. ✅ Файлы `.env` созданы для backend и frontend
3. ✅ Токен Telegram бота добавлен: `8528158151:AAEseUlWVmgA4I2lSnI3LXLcwZ4sl6oPNhE`
4. ✅ Схема базы данных применена через `prisma db push`
5. ✅ Зависимости установлены

## 🚀 Запуск проекта

### Вариант 1: Запуск обоих серверов одновременно

```bash
# Из корня проекта
npm run dev
```

### Вариант 2: Запуск отдельно

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## 📝 Текущие настройки

### Backend (.env)
- **База данных**: `postgresql://pandrosov@localhost:5432/ambassadors_tma`
- **Порт**: `3000`
- **Telegram Bot Token**: настроен ✅

### Frontend (.env)
- **API URL**: `http://localhost:3000`

## 🔧 Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Выполните команду `/newapp`
3. Выберите вашего бота
4. Укажите название приложения (например: "Ambassadors App")
5. Укажите URL: `http://localhost:5173` (для разработки) или ваш production URL
6. Опционально: добавьте описание и иконку

## 🧪 Проверка работы

1. **Backend API**: Откройте http://localhost:3000/health
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend**: Откройте http://localhost:5173
   - Должно открыться приложение

3. **Prisma Studio** (опционально): http://localhost:5555
   - Для просмотра данных в базе

## 📊 Следующие шаги

1. **Создать первого пользователя**:
   - Откройте Mini App в Telegram
   - При первом входе пользователь автоматически создастся в базе

2. **Создать тестовое задание** (через API или Prisma Studio):
   ```bash
   # Пример через curl (нужна аутентификация)
   curl http://localhost:3000/api/tasks
   ```

3. **Настроить Google Sheets** (опционально):
   - Получить credentials в Google Cloud Console
   - Добавить в `.env` файл backend

## ⚠️ Важные замечания

- **DATABASE_URL**: Если у вас другой пароль для PostgreSQL, обновите его в `backend/.env`
- **CORS**: Frontend URL настроен на `http://localhost:5173` - убедитесь, что он совпадает с вашим
- **Telegram WebApp**: Для работы в Telegram нужно развернуть frontend на HTTPS (можно использовать ngrok для разработки)

## 🐛 Решение проблем

### База данных не подключается
```bash
# Проверьте подключение
psql -U $(whoami) -d ambassadors_tma -c "SELECT 1;"
```

### Порт занят
```bash
# Измените PORT в backend/.env
# Или убейте процесс на порту 3000
lsof -ti:3000 | xargs kill
```

### Prisma ошибки
```bash
cd backend
npx prisma generate
npx prisma db push
```

