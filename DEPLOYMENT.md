# Руководство по деплою проекта

## Рекомендуемые платформы для бесплатного деплоя

### 🚀 Лучшие варианты для full-stack приложения

#### 1. **Railway** (⭐ Рекомендуется)
**Почему подходит:**
- ✅ Бесплатный тариф: $5 кредитов в месяц (достаточно для небольших проектов)
- ✅ Встроенная поддержка PostgreSQL
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка переменных окружения
- ✅ Поддержка Node.js из коробки
- ✅ HTTPS из коробки

**Ограничения:**
- После исчерпания бесплатных кредитов требуется оплата
- Может "засыпать" при отсутствии активности (но быстро просыпается)

**Инструкция:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Создайте новый проект → "Deploy from GitHub repo"
4. Выберите репозиторий
5. Railway автоматически определит Node.js проект
6. Добавьте PostgreSQL базу данных: "New" → "Database" → "PostgreSQL"
7. Настройте переменные окружения в настройках проекта
8. Railway автоматически создаст публичный URL

**Стоимость:** Бесплатно (с ограничениями)

---

#### 2. **Render**
**Почему подходит:**
- ✅ Бесплатный тариф для веб-сервисов
- ✅ Встроенная поддержка PostgreSQL
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки

**Ограничения:**
- Приложение "засыпает" после 15 минут неактивности (первый запрос может быть медленным)
- Медленнее, чем Railway

**Инструкция:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте "New Web Service"
4. Выберите репозиторий и ветку
5. Настройте:
   - **Build Command:** `cd backend && npm install && npm run build`
   - **Start Command:** `cd backend && npm start`
6. Создайте PostgreSQL базу данных: "New" → "PostgreSQL"
7. Настройте переменные окружения
8. Render создаст публичный URL

**Стоимость:** Бесплатно (с ограничениями)

---

#### 3. **Fly.io**
**Почему подходит:**
- ✅ Бесплатный тариф: 3 shared-cpu-1x VMs
- ✅ Быстрый деплой
- ✅ Поддержка PostgreSQL через дополнения

**Ограничения:**
- Требуется больше настройки
- PostgreSQL нужно настраивать отдельно

**Инструкция:**
1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. Создайте приложение: `fly launch`
4. Настройте `fly.toml`
5. Деплой: `fly deploy`

**Стоимость:** Бесплатно (с ограничениями)

---

### 🎨 Варианты для Frontend (если деплоите отдельно)

#### 4. **Vercel** (для Frontend)
**Почему подходит:**
- ✅ Отлично для React/Vite приложений
- ✅ Бесплатный тариф с хорошими лимитами
- ✅ Автоматический деплой из GitHub
- ✅ Edge Network (быстрая загрузка по всему миру)
- ✅ HTTPS из коробки

**Ограничения:**
- Только для статических сайтов и serverless функций
- Backend нужно деплоить отдельно

**Инструкция:**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Выберите папку `frontend`
4. Vercel автоматически определит Vite проект
5. Настройте переменные окружения (например, `VITE_API_URL`)
6. Деплой произойдет автоматически

**Стоимость:** Бесплатно

---

#### 5. **Netlify** (для Frontend)
**Почему подходит:**
- ✅ Отлично для статических сайтов
- ✅ Бесплатный тариф
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки

**Ограничения:**
- Только для frontend
- Backend нужно деплоить отдельно

**Стоимость:** Бесплатно

---

### 🗄️ Варианты для PostgreSQL (если деплоите отдельно)

#### 6. **Supabase**
**Почему подходит:**
- ✅ Бесплатный тариф: 500MB база данных
- ✅ PostgreSQL из коробки
- ✅ Встроенный REST API
- ✅ Реальное время (realtime subscriptions)

**Ограничения:**
- 500MB может быть недостаточно для больших проектов
- После лимита требуется оплата

**Инструкция:**
1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Получите connection string
4. Используйте в `DATABASE_URL`

**Стоимость:** Бесплатно (до 500MB)

---

#### 7. **Neon** (PostgreSQL)
**Почему подходит:**
- ✅ Бесплатный тариф: 0.5GB база данных
- ✅ Serverless PostgreSQL
- ✅ Автоматическое масштабирование
- ✅ Быстрое создание и удаление баз

**Стоимость:** Бесплатно (до 0.5GB)

---

## 🎯 Рекомендуемая архитектура деплоя

### Вариант 1: Все на Railway (Проще всего)
```
Backend (Node.js) → Railway
Frontend (React) → Railway (как статический сайт)
PostgreSQL → Railway (встроенная БД)
```

**Плюсы:**
- Все в одном месте
- Простая настройка
- Один URL для всего

**Минусы:**
- Может быть дороже при росте трафика

---

### Вариант 2: Раздельный деплой (Оптимально)
```
Backend (Node.js) → Railway или Render
Frontend (React) → Vercel или Netlify
PostgreSQL → Supabase или Neon
```

**Плюсы:**
- Оптимизация под каждый сервис
- Лучшая производительность frontend (CDN)
- Бесплатные лимиты для каждого сервиса

**Минусы:**
- Больше настройки
- Нужно управлять несколькими сервисами

---

## 📝 Подготовка к деплою

### 1. Переменные окружения

Создайте файл `.env.example` с примерами переменных:

```env
# Database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
FRONTEND_URL=https://your-frontend-url.vercel.app

# Google Sheets (если используется)
GOOGLE_SHEETS_CLIENT_EMAIL=your_email
GOOGLE_SHEETS_PRIVATE_KEY=your_key
GOOGLE_SHEETS_SPREADSHEET_ID=your_id

# JWT
JWT_SECRET=your_secret_key

# Server
PORT=3000
NODE_ENV=production
```

### 2. Обновите `package.json` для production

**Backend:**
```json
{
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  }
}
```

**Frontend:**
```json
{
  "scripts": {
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

### 3. Настройте CORS для production

В `backend/src/index.ts`:
```typescript
app.use(cors({
  origin: [
    process.env.FRONTEND_URL,
    // Добавьте ваш production URL
  ],
  credentials: true,
}));
```

### 4. Обновите Telegram Bot Webhook

После деплоя backend обновите webhook:
```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
  -d "url=https://your-backend-url.railway.app/webhook/telegram"
```

---

## 🚀 Быстрый старт с Railway

1. **Подготовка:**
   ```bash
   # Убедитесь, что код закоммичен в GitHub
   git add .
   git commit -m "Prepare for deployment"
   git push
   ```

2. **Деплой Backend:**
   - Зайдите на [railway.app](https://railway.app)
   - "New Project" → "Deploy from GitHub repo"
   - Выберите репозиторий
   - Railway определит Node.js проект
   - Добавьте PostgreSQL: "New" → "Database" → "PostgreSQL"
   - Настройте переменные окружения
   - Railway создаст URL типа `https://your-app.up.railway.app`

3. **Деплой Frontend:**
   - В том же проекте Railway создайте новый сервис
   - Выберите папку `frontend`
   - Настройте:
     - **Root Directory:** `frontend`
     - **Build Command:** `npm install && npm run build`
     - **Start Command:** `npx serve -s dist`
   - Или деплойте на Vercel (рекомендуется для frontend)

4. **Настройка переменных:**
   - В Railway: Settings → Variables
   - Добавьте все переменные из `.env.example`
   - Обновите `FRONTEND_URL` на URL вашего frontend

---

## 📊 Сравнение платформ

| Платформа | Backend | Frontend | PostgreSQL | Бесплатный тариф | Рекомендация |
|-----------|---------|----------|------------|-----------------|--------------|
| Railway   | ✅      | ✅       | ✅         | $5/мес кредиты   | ⭐⭐⭐⭐⭐ |
| Render    | ✅      | ✅       | ✅         | Бесплатно        | ⭐⭐⭐⭐ |
| Fly.io    | ✅      | ✅       | ⚠️         | 3 VMs            | ⭐⭐⭐ |
| Vercel    | ⚠️      | ✅       | ❌         | Бесплатно        | ⭐⭐⭐⭐ (только frontend) |
| Netlify   | ⚠️      | ✅       | ❌         | Бесплатно        | ⭐⭐⭐ (только frontend) |
| Supabase  | ❌      | ❌       | ✅         | 500MB            | ⭐⭐⭐⭐ (только БД) |
| Neon      | ❌      | ❌       | ✅         | 0.5GB            | ⭐⭐⭐⭐ (только БД) |

---

## 🔧 Полезные команды

### Railway CLI
```bash
npm i -g @railway/cli
railway login
railway init
railway up
```

### Проверка деплоя
```bash
# Проверка backend
curl https://your-backend-url.railway.app/health

# Проверка frontend
curl https://your-frontend-url.vercel.app
```

---

## ⚠️ Важные замечания

1. **Telegram Mini Apps требуют HTTPS** - убедитесь, что используете HTTPS URL
2. **CORS настройки** - добавьте production URL в CORS
3. **Переменные окружения** - никогда не коммитьте `.env` файлы
4. **База данных** - сделайте резервные копии перед миграциями
5. **Мониторинг** - используйте встроенные логи платформ для отладки

---

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Supabase Documentation](https://supabase.com/docs)

---

**Рекомендация:** Начните с Railway для всего проекта - это самый простой способ получить работающее приложение. Позже можно оптимизировать, разделив frontend на Vercel для лучшей производительности.

