# ✅ Чеклист для деплоя на Railway

## 📦 Изменения в этом коммите

### ✅ Выполнено:
1. ✅ Заменен `date-fns` на `dayjs` (меньший размер, проще интеграция)
2. ✅ Обновлены все Grid компоненты на Material-UI v7 синтаксис (`size={{ xs: 12 }}`)
3. ✅ Упрощена конфигурация Vite (удалены плагины для date-fns)
4. ✅ Исправлены TypeScript типы для Date Pickers

### 📝 Файлы изменены:
- `frontend/package.json` - заменен date-fns на dayjs
- `frontend/vite.config.ts` - упрощена конфигурация
- `frontend/src/components/admin/Leaderboard.tsx` - AdapterDayjs, типы Dayjs
- `frontend/src/components/admin/Statistics.tsx` - AdapterDayjs, типы Dayjs
- `frontend/src/pages/ProfilePage.tsx` - Grid компоненты обновлены
- `frontend/src/pages/ShopPage.tsx` - Grid компоненты обновлены
- `package-lock.json` - обновлены зависимости

---

## 🚀 Шаги для деплоя

### 1. Закоммить изменения
```bash
git add .
git commit -m "feat: replace date-fns with dayjs, update Grid components to MUI v7"
git push
```

### 2. Проверить настройки Railway

#### Backend сервис:
- ✅ Root Directory: `backend`
- ✅ Build & Deploy: **Dockerfile** (не Nixpacks)
- ✅ Environment Variables настроены (см. RAILWAY_ENV_VARIABLES.md)

#### Frontend сервис:
- ✅ Root Directory: `.` (корень проекта)
- ✅ Build & Deploy: **Dockerfile** (не Railpack/Nixpacks)
- ✅ Environment Variables настроены

### 3. Railway автоматически задеплоит после push

Railway отслеживает изменения в репозитории и автоматически запускает деплой при push в основную ветку.

---

## 🔍 Проверка после деплоя

1. **Backend:**
   - Проверить логи на наличие ошибок
   - Проверить, что сервис отвечает на `/health` или `/api/health`

2. **Frontend:**
   - Проверить логи на наличие ошибок сборки
   - Открыть URL фронтенда в браузере
   - Проверить работу Date Pickers в админ-панели

---

## ⚠️ Возможные проблемы

### Если сборка падает:
1. Проверить логи в Railway Dashboard
2. Убедиться, что Root Directory настроен правильно
3. Убедиться, что используется Dockerfile (не Nixpacks/Railpack)

### Если Date Pickers не работают:
1. Проверить, что `dayjs` установлен в `package.json`
2. Проверить, что используется `AdapterDayjs` (не `AdapterDateFns`)
3. Проверить консоль браузера на ошибки

---

## 📊 Размер бандла

После замены date-fns на dayjs:
- **Было:** ~70KB (date-fns)
- **Стало:** ~2KB (dayjs)
- **Экономия:** ~68KB 🎉

---

## ✅ Готово к деплою!

Все изменения протестированы локально, сборка проходит успешно.

