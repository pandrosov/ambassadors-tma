# 🔍 Отладка данных Telegram

## Проблема: Telegram user: null

Если в логах видно `Telegram user: null`, это означает, что данные пользователя не передаются из Telegram.

## Решение:

### 1. Проверьте в консоли браузера:

Откройте DevTools в Telegram Desktop (F12) и выполните в консоли:

```javascript
// Проверка наличия Telegram WebApp
window.Telegram?.WebApp

// Проверка initData
window.Telegram?.WebApp?.initData

// Проверка initDataUnsafe
window.Telegram?.WebApp?.initDataUnsafe

// Проверка пользователя
window.Telegram?.WebApp?.initDataUnsafe?.user
```

### 2. Проверьте window.location:

```javascript
// Проверка hash параметров
window.location.hash

// Проверка search параметров
window.location.search
```

### 3. Если данные не передаются:

Возможные причины:
1. **Bot не настроен правильно** - проверьте настройки в BotFather
2. **Mini App открыт не из Telegram** - убедитесь, что открываете через бота
3. **Версия Telegram** - обновите Telegram до последней версии

### 4. Временное решение:

Если данные все равно не передаются, backend автоматически создаст пользователя при первом запросе (в режиме разработки).

Проверьте логи backend:
```bash
tail -f /tmp/backend.log | grep "Creating dev user"
```

## Логи для проверки:

В консоли должны быть логи:
- `Telegram WebApp инициализирован` - с данными о версии и платформе
- `Got user from initDataUnsafe:` - если пользователь найден
- `Got initData from Telegram.WebApp:` - если initData найден

Если этих логов нет, значит Telegram WebApp API не загружен или не инициализирован.

